import Spinner from '../Spinner/Spinner';

const ServerList = () => {
  return (
    <div className="bg-black/60 w-full h-full flex flex-col items-center gap-4">
      ¯\_(ツ)_/¯
      <Spinner size={32}/>
    </div>
  );
};

export default ServerList;
